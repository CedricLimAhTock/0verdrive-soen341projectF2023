apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    orgId: 1
    # Set this to your systems local ip address, not 127.0.0.1 nor localhost
    url: http://192.168.0.10:9090
    basicAuth: false
    isDefault: true
    editable: true